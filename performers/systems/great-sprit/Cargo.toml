[package]
name = "great-sprit"
version = "0.1.0"
edition = "2021"
authors = ["Jumma Kawasaki"]
description = "Great Spirit GPU Physics System - Real-time spirit position calculation with GPU acceleration"
license = "MIT"

[dependencies]
# GPU computing
wgpu = "0.20"
bytemuck = { version = "1.14", features = ["derive"] }

# Visualization (Bevy)
bevy = { version = "0.13", features = [
    "default",
] }

# GraphQL API
async-graphql = "6.0"
async-graphql-warp = "6.0"
tokio = { version = "1.0", features = ["full"] }

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# Vector math
nalgebra = "0.33"

# TerminusDB client
reqwest = { version = "0.12", features = ["json"] }

# Configuration
config = "0.14"
toml = "0.8"

# Error handling
anyhow = "1.0"
thiserror = "1.0"

# Logging
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }

# Async utilities
futures = "0.3"

[dev-dependencies]
criterion = "0.5"
tokio-test = "0.4"

[[bin]]
name = "great-sprit"
path = "src/main.rs"

