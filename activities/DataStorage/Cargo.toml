[package]
name = "spirit-activity-datastorage"
version = "0.1.0"
edition = "2021"
authors = ["Spirit in Physics Team"]
description = "DataStorage Activity implementation"
license = "MIT"

[dependencies]
spirit-activities-shared = { path = "../shared" }
async-trait = "0.1"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
chrono = { version = "0.4", features = ["serde"] }

[lib]
name = "spirit_activity_datastorage"
path = "src/datastorage.rs"
