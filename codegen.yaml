schema: http://localhost:8080/graphql
documents: "**/*.graphql"
generates:
  src/generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typed-document-node"
    config:
      scalars:
        DateTime: string
        UUID: string

