apiVersion: v1
kind: Service
metadata:
  name: api
  namespace: spirit-in-physics
spec:
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: http
  selector:
    app.kubernetes.io/name: api
