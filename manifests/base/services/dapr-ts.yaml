apiVersion: v1
kind: Service
metadata:
  name: dapr-ts
  namespace: spirit-in-physics
spec:
  ports:
  - name: http
    port: 3002
    protocol: TCP
    targetPort: http
  selector:
    app.kubernetes.io/name: dapr-ts
