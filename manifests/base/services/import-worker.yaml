apiVersion: v1
kind: Service
metadata:
  name: import-worker
  namespace: spirit-in-physics
spec:
  ports:
  - name: http
    port: 80
    protocol: TCP
    targetPort: http
  selector:
    app.kubernetes.io/name: import-worker
