version: '3'

tasks:
  apply-local:
    desc: Apply manifests to local cluster using Kustomize
    cmds:
      - timeout 60s kubectl kustomize manifests/overlays/local | kubectl apply -f -

  push-manifest:
    desc: Render and push manifests for GKE to Artifact Registry
    cmds:
      - mkdir -p .build/manifests
      - timeout 30s kubectl kustomize manifests/overlays/gke > .build/manifests/all.yaml
      - |
        cat <<EOF > .build/manifests/Dockerfile
        FROM scratch
        COPY all.yaml /
        EOF
      - timeout 300s docker build -t asia-northeast1-docker.pkg.dev/com-junkawasaki-sip/spirit-in-physics/manifests:latest .build/manifests
      - timeout 60s docker push asia-northeast1-docker.pkg.dev/com-junkawasaki-sip/spirit-in-physics/manifests:latest

  push-app:
    desc: Build and push svelte-app image to Artifact Registry
    cmds:
      - timeout 600s docker build --platform linux/amd64 -t asia-northeast1-docker.pkg.dev/com-junkawasaki-sip/spirit-in-physics/svelte-app:latest apps/svelte-app
      - timeout 120s docker push asia-northeast1-docker.pkg.dev/com-junkawasaki-sip/spirit-in-physics/svelte-app:latest
      - timeout 30s kubectl rollout restart deployment portal -n spirit-in-physics

