{
  "name": "api-worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit",
    "db:migrate:local": "wrangler d1 migrations apply spirit-in-physics --local",
    "db:migrate:remote": "wrangler d1 migrations apply spirit-in-physics --remote"
  },
  "dependencies": {
    "@langchain/langgraph": "^1.3.0",
    "@simplewebauthn/server": "^11.0.0",
    "hono": "^4.8.3",
    "kysely": "^0.28.8",
    "kysely-d1": "^0.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260405.0",
    "typescript": "^5.9.3",
    "wrangler": "^4.12.0"
  }
}
