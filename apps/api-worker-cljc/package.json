{
  "name": "@spirit/api-worker-cljc",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "org-spirit-in-physics apps/api-worker, CLJC port (shadow-cljs :esm fetch worker)",
  "scripts": {
    "test": "amu compile --target wasm32-browser worker-test && node out/worker-tests.cjs",
    "watch-test": "amu compile --target wasm32-browser worker-test",
    "build:worker": "amu compile --target wasm32-browser worker",
    "watch:worker": "amu compile --target wasm32-browser worker",
    "smoke": "node smoke.mjs",
    "verify": "amu compile --target wasm32-browser worker-test && node out/worker-tests.cjs && amu compile --target wasm32-browser worker && node smoke.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply spirit-in-physics --local",
    "db:migrate:remote": "wrangler d1 migrations apply spirit-in-physics --remote"
  },
  "devDependencies": {
    "shadow-cljs": "^2.28.3",
    "wrangler": "^4.12.0"
  },
  "dependencies": {
    "@simplewebauthn/server": "^11.0.0"
  }
}
