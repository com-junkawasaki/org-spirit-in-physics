{
  "name": "@spirit/web-cljc",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "org-spirit-in-physics apps/web + apps/researcher, CLJC port (shadow-cljs :browser, cljc-ui-IR, Pattern A assets-only SPA)",
  "scripts": {
    "test": "amu compile --target wasm32-browser web-test && node out/web-tests.cjs",
    "watch-test": "amu compile --target wasm32-browser web-test",
    "build:web": "amu compile --target wasm32-browser web",
    "watch:web": "amu compile --target wasm32-browser web",
    "build:researcher": "amu compile --target wasm32-browser researcher",
    "watch:researcher": "amu compile --target wasm32-browser researcher",
    "build": "amu compile --target wasm32-browser web researcher",
    "verify": "amu compile --target wasm32-browser web-test && node out/web-tests.cjs && amu compile --target wasm32-browser web researcher",
    "serve:web": "sirv-cli public/web --single --port 5173",
    "serve:researcher": "sirv-cli public/researcher --single --port 5174"
  },
  "devDependencies": {
    "shadow-cljs": "^2.28.3",
    "sirv-cli": "^3.0.1"
  },
  "dependencies": {
    "@simplewebauthn/browser": "^11.0.0",
    "d3": "^7.9.0",
    "katex": "^0.16.47"
  }
}
