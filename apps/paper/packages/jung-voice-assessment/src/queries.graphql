query GetStimulusWords {
  stimulusWords {
    id
    japanese
    english
    pronunciation
  }
}

query GetStimulusWord($id: Int!) {
  stimulusWord(id: $id) {
    id
    japanese
    english
    pronunciation
  }
}

