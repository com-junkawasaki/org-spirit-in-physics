---
// Merkle DAG: components.research_paper
// Main research paper layout component with JSON-LD annotations

import { getCombinedContext } from '../lib/semantic/load-schemas';

interface Props {
  title: string;
  description: string;
  authors: Array<{ name: string; email?: string; affiliation: string }>;
  date: string;
  affiliations: string[];
  schemaId?: string;
}

const { title, description, authors, date, affiliations, schemaId } = Astro.props;
const schemas = getCombinedContext();
---

<article class="prose prose-lg dark:prose-invert max-w-4xl mx-auto px-4 py-8">
  <header class="mb-8">
    <h1 class="text-4xl font-bold mb-4">{title}</h1>
    <p class="text-xl text-gray-600 dark:text-gray-400 mb-6">{description}</p>
    
    <div class="mb-4">
      <strong>Authors:</strong>
      {authors.map((author, index) => (
        <>
          {author.name}
          {author.email && (
            <a href={`mailto:${author.email}`} class="ml-1">({author.email})</a>
          )}
          {index < authors.length - 1 && ', '}
        </>
      ))}
    </div>
    
    <div class="mb-4">
      <strong>Affiliation:</strong> {affiliations.join(', ')}
    </div>
    
    <time datetime={date} class="text-sm text-gray-500">
      {new Date(date).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' })}
    </time>
  </header>
  
  <slot />
</article>

<script type="application/ld+json" set:html={JSON.stringify(schemas, null, 2)} />

